// priceReminder.js
// a widget to show XAUUSD/XAGUSD/Au(T+D)/Ag(T+D) realtime price
// only run in `Scriptable` app

const $http = importModule("http.module")
const $cache = importModule("cache.module")